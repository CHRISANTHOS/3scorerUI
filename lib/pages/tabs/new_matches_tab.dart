import 'package:flutter/material.dart';

class NewMatches extends StatelessWidget {
  const NewMatches({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('New Matches Tab'),
      ),
    );
  }
}