import 'package:flutter/material.dart';

class PostMatches extends StatelessWidget {
  const PostMatches({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Post Matches Tab'),
      ),
    );
  }
}